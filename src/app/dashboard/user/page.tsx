import AppDashboard from "@/components/dashboards/dashboard";
import React from "react";

export default function User() {
	return <AppDashboard content={<div>ini konten user</div>} activeKey={"user"} />;
}
